global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "rules/*.yml"

scrape_configs:
  - job_name: 'tempo'
    static_configs:
      - targets:
          - 'tempo:3200'

  - job_name: 'postgres'
    static_configs:
      - targets:
          - 'postgres-exporter:9187'
